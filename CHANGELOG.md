## v0.2.0（2026-08-14）· 前端桌面端 UI 优化

### 新增
- 全局设计系统（`frontend/src/styles/main.css`）：CSS 变量统一颜色/圆角/阴影/间距/字体，通用按钮/输入框/状态徽章类
- 桌面应用壳（`AppLayout.vue`）：左侧固定侧边栏导航 + 右侧自适应主区；窄屏（<768px）侧边栏自动收起为顶部横条

### 优化
- 登录页改居中卡片式登录
- 对话页改气泡式消息流（用户消息右对齐气泡 / Agent 反馈左卡片），发送后自动滚动到底
- 结果卡片分区块：状态徽章 + 步骤编号 + 高危标记 + 每步结果
- 待确认/历史页卡片化 + 状态彩色徽章 + 行 hover 高亮
- 确认弹窗宽度自适应（min(480px, 90vw)）+ 淡入缩放动画
- 设置页放宽布局，表单聚焦高亮
- 所有按钮/输入框补充 hover/focus/disabled 状态与过渡动画

### 说明
- 未引入 UI 组件库，保持零第三方依赖；后端与接口层未改动

---
## v0.1.1（2026-08-14）

### 新增
- 任务绑定用户：创建/去重/历史按用户隔离，确认防串账（他人任务不可操作）
- 前端任务历史页（「历史」入口，可展开看子任务状态）
- 每人绑定自己的 QQ 邮箱（「我的」设置页）：未绑定自动用系统默认邮箱
- 邮箱配置接口：保存 / 打码读取 / 测试发送

### 安全说明
- 用户授权码存数据库为明文（MVP 阶段），后续应加密存储或改 OAuth 方式

---
# 变更记录（Changelog）

## v0.1.0（2026-08-14）· MVP

### 功能
- 飞书登录 + 令牌自动刷新
- 自然语言意图拆解 → 子任务（反问澄清、追问上限 3 轮）
- 工具执行：QQ 邮箱（读/发）、飞书日历（建/改日程）、本地 Excel/CSV（读/写）
- 高危动作确认闸门：删除/覆盖/发送/外发/更新 必须先确认，超时挂起，拒绝不执行
- 任务落库 + 按用户隔离的历史记录（前端「历史」页）
- 改表格按表头定位（说“张三的电话”自动找列/行），写前生成变更预览

### 安全与健壮性
- 参数校验（邮箱/changes/时间格式）、敏感操作白名单、表格目录隔离
- 5 分钟内同文本去重复用、确认幂等
- 日志落盘（logs/app.log）+ 统一异常处理（不崩、原因可查）
- API Key 存 .env，不进版本库

### 测试
- 后端单测 83 个
- 自动化黄金测试 20 Case / 3 评分器 / 完整 Trace（实测总分 99.4）

### 已知限制（后续版本）
- 邮件为单账号（所有发信来自测试邮箱）；每人绑定邮箱待做
- 数据库为 SQLite（多人可换 PostgreSQL）
- 仅中文界面